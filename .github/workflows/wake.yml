name: Keep App Alive
on:
  schedule:
    - cron: "0 */3 * * *" # Runs every 3 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -I https://q-voter-app-app-54xgh5gxxrr4pukoxwfmwe.streamlit.app/
